package env

const (
    // paths
    CONFIG_PATH = "macro.yml"

    // button pad specs
    PAD_BUTTONS = 16
    PAD_LAYERS  = 4

    // defaults
    DEFAULT_LAYER  = "0"
    DEFAULT_LAYOUT = "qwerty_us"

    // environment keys

    LAYOUT = "layout"
    LAYER  = "default_layer"
)